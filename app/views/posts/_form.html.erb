<%= form_with(model: post, url: posts_path, local: true) do |f| %>
  <%= render "shared/error_messages", object: post %>

  <%= f.text_area :content, size: "60x1", placeholder: "What's on your mind?" %><br>
  <%= f.submit "Create post" %>
<% end %>
